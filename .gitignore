# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp

# Nothing here is built — every file in this repo is served as-is.

# Local metadata tool — never ships
admin_pixmakers.html
work-metadata.json
WORK-METADATA.md
_devserver.js
work-metadata.json.bak
_addwork.py
_stamp.py
_apply.py

# Depot de travail : sources brutes deposees a la main — logos non detoures,
# images pleine resolution. Ce qui doit partir en ligne est retaille et range
# dans assets/, jamais servi depuis ici.
public/
